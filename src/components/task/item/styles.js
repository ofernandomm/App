import { StyleSheet } from "react-native";
export const styles=StyleSheet.create({
    listItem: {
        fontSize: 14,
        color: '#ffffff',
        paddingHorizontal: 10,
    },
    listItemCompleted:{
        fontSize: 14,
        color: '#e62e1b',
        paddingHorizontal: 10,
    }
})